[project]
name = "ant"
version = "0.1.0"
description = "Anthropic's Demo"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.91.0",
    "python-dotenv>=1.2.2",
]

[dependency-groups]
dev = [
    "ipykernel>=7.2.0",
]
